# Environment variables for Panneau Solaire
# Copy this file to .env and adjust the values.
# NEVER commit the real .env file.

# SQL Server password (must meet SQL Server complexity requirements)
SA_PASSWORD=PanneauSolaire2024!

# Database connection (used by the Python app)
DB_SERVER=sqlserver
DB_PORT=1433
DB_NAME=PanneauSolaire
DB_USER=sa
DB_PASSWORD=PanneauSolaire2024!
